
get:
  tags:
    - RoteiroRepository
  operationId: "RoteiroRepository_findAll"
  parameters:
    - in: query
      name: termo
      required: false
      type: "string"
    - in: query
      name: page
      required: true
      type: "integer"
      format: "int64"
    - in: query
      name: count
      required: true
      type: "integer"
      format: "int64"
  responses:
    "200":
      description: roteiros turísticos encontrados
      schema:
        type: "array"
        items:
          $ref: "./model/roteiro.yaml#/Roteiro"
    default:
      description: erro ocorrido

