
definitions:

  create:

    post:
      tags:
        - Conta
      operationId: 'Conta_criar'
      parameters:
        - in: body
          name: 'conta'
          required: true
          schema:
            $ref: './model/conta.yaml#/Conta'
      responses:
        '204':
          description: conta criada
        default:
          description: erro ocorrido


